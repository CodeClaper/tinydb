#include <sched.h>

#define INT64CONST(x)  (x##L)
#define UINT64CONST(x) (x##UL)

/* Size 
 * Size of any memory resident object.
 */
typedef size_t Size;

/* Pid
 * The processor id.
 * */
typedef pid_t Pid;
